<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorWhiteGrey</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.Launcher" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="NumbersButtons" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="android:textSize">@dimen/textSizeUpperButtons</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="autoSizeTextType">uniform</item>
    </style>

    <style name="FunctionalButtons" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="android:textSize">30dp</item>
        <item name="autoSizeTextType">uniform</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">@color/colorBlack</item>
    </style>

</resources>